# 2026-07-25 工作日志

## 大连+沈阳7天行程 HTML 指南（GitHub Pages: amx708.github.io/dalian-trip-2026，本地 dalian-deploy/）
- 修复「景点详解」沈阳景点卡片丢失问题：早前 commit 048bb51 清理排版时误删了含全部沈阳景点卡片的「沈阳 2 日最终方案」段落。本日从 `git show 048bb51^:cloud.html` 恢复沈阳 9 个景点卡片（沈阳故宫/张氏帅府/九一八/辽博/沈阳博物馆/西塔/中街/工业博物馆/彩电塔），插入为「🏯 沈阳段 · Day4–6（路线最优先方案）」子标题下的 grid。
- 处理过程中发现并修复源码缺陷：① 辽博卡片存在重复的嵌套 `<div class="spot"><div class="hd">` 开头（已去重）；② 源码末尾有一个无实际闭合的游离 `<h2>` 开启标签被误并入提取范围（已截断）；③ 西塔卡片在原始源码中无前导缩进，提取需按 `<div class="spot">` 任意缩进匹配；④ 去除重复的「辽宁省博物馆」纯文字卡片。
- 新增大连 Day4 卡片「🚄 大连北站（G8101 10:56 出发）」到景点详解（Dalian grid 末尾），满足用户“大连的day4到高铁站也写出来”的要求。
- 验证：9 张沈阳卡 + 大连北站卡均平衡（div 计数），无重复 h2，全卡本地校验存在。文件整体 +1 div 为历史遗留（HEAD 已有），浏览器可正常渲染。
- 提交 4fcdc50 并 push 成功（github.com:443 偶发超时，已重试）。

## 技术注意
- 该 HTML 单文件景点卡片用 `extract_cards` 式平衡扫描提取时，必须按 `<div class="spot">` 而非带缩进的 `      <div class="spot">` 匹配，否则会漏掉无缩进卡片（西塔）。
- Git push 到 github.com 常因 443 超时失败，重试即可；GitHub Pages CDN 约 40–90s 缓存，用户需硬刷新（Ctrl+Shift+R）。

## 沈阳段按地图"同圈同天"原则重新 clustering
- 起因：用户指出沈阳故宫与张氏帅府（张学良旧居）相邻却被拆到 Day4/Day5，被家人吐槽；并提供沈阳 4 圈住宿/景点图（左上：中街/故宫/张学良旧居；右上：九一八/西塔等；左下：辽博/科技馆；右下：棋盘山）。
- 调整方案：
  - **Day4**（老城核心圈）：沈阳故宫 14:00–16:00 → 张氏帅府 16:15–17:45 → 沈阳博物馆 18:00–19:00 → 中街 19:00–20:30，全部相邻步行/短距离，避免来回跑。
  - **Day5**（东北/西塔圈 + 工业博物馆）：九一八历史博物馆 9:00–10:30 → 中国工业博物馆 13:30–15:30 → 西塔韩餐晚餐 17:30–19:00 → 彩电塔夜市 20:00–21:00。
  - **Day6**（浑南辽博圈）：辽宁博物馆（辽博）9:00–12:30 → 简餐 → 16:53 G8208 沈阳站回大连。
- 文件修改：
  - `cloud.html`：重写 Day4/Day5/Day6 行程表；更新 8 张景点卡片的 `plan-tag`/`onclick`/`📅 高亮`/`bd` 文案（帅府/九一八/辽博/沈博/西塔/中街/工业博物馆）；更新顶部 note 与顺路提示。
  - `map.html`：更新 POIS_PLAN1/PLAN2 中 6 个沈阳 POI 的 `day` 字段（帅府/沈博/中街 5→4，九一八/工业博物馆/西塔 按新计划调整），并同步更新 `DAY_LABEL_PLAN1/PLAN2` 的沈阳段标签。
  - `大连沈阳7天初步方案.txt`：同步骨架到新版 Day4/Day5/Day6。
- 验证：`check_syntax.js` 校验 cloud.html / map.html 均 SYNTAX OK；cloud.html div 计数保持 379/378（与 HEAD 一致的预存 +1，新增内容自身平衡）。

## 修正沈阳 POI 时间/日期/名称（map.html 侧边栏与 cloud.html 数组同步）
- 起因：用户截图显示 map.html 侧边栏里张氏帅府仍显示 `9:00–10:30`，与新版 clustering 不一致；检查发现 `cloud.html` 和 `map.html` 里的 `POIS_PLAN2` 数组时间/日期仍为旧版。
- 修正内容（统一为新版 clustering）：
  - `shuaifu`：day 5→4（cloud.html），time `9:00–10:30`→`16:15–17:45`，name `张氏帅府`→`张氏帅府（张学良旧居）`。
  - `shenyang_museum`：day 5→4（cloud.html），time `10:45–11:45`→`18:00–19:00`。
  - `zhongjie`：day 5→4（cloud.html），time `12:30–17:00`→`19:00–20:30`。
  - `xita`：day 4→5（cloud.html），name `西塔(韩餐夜市)`→`西塔（朝鲜族风情街）`。
  - `918`：day 6→5（cloud.html）。
  - `industrial_museum`：day 6→5（cloud.html），time `14:30–16:00`→`13:30–15:30`。
  - `liaoning_museum`：time `11:00–13:30`→`9:00–12:30`。
  - `caita_night`：time `19:30–21:00`→`20:00–21:00`。
  - `shenyang_st`：time `11:00–12:30 抵达`→`12:28 抵达`。
- 文件修改：`cloud.html`、`map.html` 的 `var POIS_PLAN2` 数组均用 Python json 解析+重写完成。
- 验证：`check_syntax.js` 校验 cloud.html / map.html 均 SYNTAX OK；提交 `0fd6c90` 并 push 成功。
